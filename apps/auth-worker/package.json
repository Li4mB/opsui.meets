{
  "name": "@opsui/auth-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@opsui/db": "workspace:*",
    "@opsui/shared-types": "workspace:*",
    "@sentry/cloudflare": "10.47.0"
  },
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}

{
  "name": "@opsui/media-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@opsui/media-adapter": "workspace:*"
  },
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
